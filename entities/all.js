export const Package = {
  list: async () => {
    // Returning an empty array will trigger the default packages in the component
    return [];
  }
};

export const Testimonial = {
  list: async () => {
    // Returning an empty array will trigger the default testimonials in the component
    return [];
  }
}; 